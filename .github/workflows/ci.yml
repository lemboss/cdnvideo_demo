name: CI/CD

on:
  pull_request:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Check formatting
        run: ruff format --check app tests
      - name: Run lint
        run: ruff check app tests
      - name: Run tests
        run: pytest

  docker:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: [test, docker]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   environment: production
  #   steps:
  #     - name: Deploy over SSH
  #       uses: appleboy/ssh-action@v1.2.0
  #       env:
  #         APP_IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}
  #         GHCR_USERNAME: ${{ github.actor }}
  #         GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         port: ${{ secrets.DEPLOY_PORT || '22' }}
  #         envs: APP_IMAGE,GHCR_USERNAME,GHCR_TOKEN
  #         script: |
  #           set -euo pipefail
  #           cd "${{ secrets.DEPLOY_PATH }}"
  #           echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USERNAME" --password-stdin
  #           export APP_IMAGE="$APP_IMAGE"
  #           docker compose pull api
  #           docker compose up -d --no-build --remove-orphans api redis
  #           docker compose ps
  #           curl --fail --retry 12 --retry-delay 5 --max-time 5 http://127.0.0.1:8000/health
